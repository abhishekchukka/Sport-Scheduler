<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>updateform</title>
    <link rel="stylesheet" href="/css/output.css" />
  </head>
  <body
    class="bg-gray-100 flex flex-col items-center justify-center min-h-screen"
  >

    <section>
      <h2 class="text-2xl font-semibold mb-4">Update Session</h2>
      
      <form
        method="POST"
        action="/session/<%= session.id%>"
        class="bg-white shadow rounded p-6 min-w-96"
      >
        <div class="mb-4">
          <label for="venue" class="block text-gray-700 font-medium mb-2"
            >venue</label
          >
          <input
            type="text"
            id="venue"
            name="venue"
            value="<%= session.venue %>"
            placeholder="<%= session.venue %>"
            class="w-full px-3 py-2 border rounded text-gray-700"
            required
          />
        </div>

        <div class="mb-4">
          <label for="date" class="block text-gray-700 font-medium mb-2"
            >Date</label
          >
          <input
            type="datetime-local"
            id="date"
            name="date"
            value="<%= session.date %>"
            class="w-full px-3 py-2 border rounded text-gray-700"
            required
          />
        </div>

        <div class="mb-4">
          <label for="participants" class="block text-gray-700 font-medium mb-2"
            >Maximum Participants</label
          >
          <input
            type="number"
            id="participants"
            name="participants"
            value="<%= session.participants %>"
            class="w-full px-3 py-2 border rounded text-gray-700"
            min="1"
            required
          />
        </div>

        <div class="mt-6">
          <button
            type="submit"
            class="w-full bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600"
          >
            Update Session
          </button>
        </div>
      </form>
    </section>
  </body>
</html>
